import Ember from 'ember';

export default Ember.Controller.extend({
	actions: {
		updateTestState(testId, newTestState) {
			
			alert('Not Implented yet, but we progress');
			//var testToUpdate = this.get('store').peekRecord('test', testId);
		}
	}
});
